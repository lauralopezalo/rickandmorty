import axios from 'axios';


/**
 * The function `getDetails` is an asynchronous function that makes a GET request to a specified URL
 * and returns the response data.
 * @param url - The `url` parameter is a string that represents the URL of the API endpoint from which
 * you want to retrieve data.
 * @returns The response object is being returned.
 */
// const getDetails = async (url) => {

//   try {
//     const response = await axios.get(url);
//     console.log(response.data.results)
//     console.log("url=>" + url)
//     return response;

//   } catch (error) {
//     console.error(error);
//     throw new Error('Error al obtener detalles');
//   }

// }

// export default getDetails;
